// Test that syscalls work inside unsafe blocks

fn main() -> i32 {
    // This should work - sys_getpid inside unsafe
    let pid: i32 = 0
    unsafe {
        pid = sys_getpid()
    }

    if pid > 0 {
        print("Got valid PID")
    }

    return 0
}
