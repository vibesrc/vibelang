// Option enum with data variant

enum Option<T> {
    Some(T),
    None,
}

fn unwrap<T>(o: Option<T>) -> T {
    match o {
        Option.Some(val) => val,
        Option.None => 0,
    }
}

fn main() -> i32 {
    let x = Option<i32>.Some(42)
    return unwrap<i32>(x)
}
