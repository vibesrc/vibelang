// Option enum with data variant

enum Option {
    Some(i32),
    None,
}

fn unwrap(o: Option) -> i32 {
    match o {
        Option.Some(val) => val,
        Option.None => 0,
    }
}

fn main() -> i32 {
    let x = Option.Some(42)
    return unwrap(x)
}
