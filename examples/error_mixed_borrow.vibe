// Should ERROR: can't mix & and ~

struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let p = Point { x: 1, y: 2 }
    let r1 = &p
    let r2 = ~p         // ERROR: already borrowed
    return 0
}
