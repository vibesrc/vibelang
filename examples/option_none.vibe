// Option enum test - None case

enum Option<T> {
    Some(T),
    None,
}

fn unwrap_or<T>(o: Option<T>, default: T) -> T {
    match o {
        Option.Some(val) => val,
        Option.None => default,
    }
}

fn main() -> i32 {
    let x = Option.None
    return unwrap_or(x, 99)
}
