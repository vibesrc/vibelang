// Option enum test - None case

enum Option {
    Some(i32),
    None,
}

fn unwrap_or(o: Option, default: i32) -> i32 {
    match o {
        Option.Some(val) => val,
        Option.None => default,
    }
}

fn main() -> i32 {
    let x = Option.None
    return unwrap_or(x, 99)
}
