// Test arrays with .len()

fn main() -> i32 {
    let nums = [1, 2, 3, 4, 5]
    let total = 0
    let i = 0
    while i < nums.len() {
        total = total + nums[i]
        i = i + 1
    }
    return total    // should be 15
}
