// Read-only borrow test

struct Point {
    x: i32,
    y: i32,
}

fn print_point(p: &Point) {
    print_int(p.x)
    print_int(p.y)
}

fn main() -> i32 {
    let p = Point { x: 10, y: 20 }
    print_point(&p)
    print_point(&p)  // can borrow twice
    return p.x       // still own it
}
