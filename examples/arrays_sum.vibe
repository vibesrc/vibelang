// Test arrays with sum function

fn sum(arr: &i32[5]) -> i32 {
    let total = 0
    let i = 0
    while i < 5 {
        total = total + arr[i]
        i = i + 1
    }
    return total
}

fn main() -> i32 {
    let nums = [1, 2, 3, 4, 5]
    return sum(&nums)    // should be 15
}
