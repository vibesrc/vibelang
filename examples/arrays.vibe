// Test arrays

fn main() -> i32 {
    let nums = [1, 2, 3, 4, 5]
    let total = 0
    let i = 0
    while i < 5 {
        total = total + nums[i]
        i = i + 1
    }
    print_int(total)
    return total    // should be 15
}
