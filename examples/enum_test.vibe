// Test all enum variants

enum Color {
    Red,
    Green,
    Blue,
}

fn color_value(c: Color, other_thing: u8) -> i32 {
    match c {
        Color.Red => 10,
        Color.Green => 20,
        Color.Blue => 30,
    }
}

fn main() -> i32 {
    let r = color_value(Color.Red)
    let g = color_value(Color.Green)
    let b = color_value(Color.Blue)
    let a = color_value(Color.Red)  // just to test other_thing parameter
    return r + g + b
}
