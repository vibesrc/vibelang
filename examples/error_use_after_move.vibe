// Should ERROR: use after move

struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let p = Point { x: 1, y: 2 }
    let q = p           // p moved to q
    return p.x          // ERROR: p was moved
}
